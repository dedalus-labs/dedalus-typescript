{
  "dxt_version": "0.2",
  "name": "dedalus-mcp",
  "version": "0.0.3",
  "description": "The official MCP Server for the Dedalus API",
  "author": {
    "name": "Dedalus",
    "email": "oss@dedaluslabs.ai"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dedalus-labs/dedalus-typescript.git"
  },
  "homepage": "https://github.com/dedalus-labs/dedalus-typescript/tree/main/packages/mcp-server#readme",
  "documentation": "https://docs.dedaluslabs.ai",
  "server": {
    "type": "node",
    "entry_point": "index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/index.js"],
      "env": {
        "DEDALUS_API_KEY": "${user_config.DEDALUS_API_KEY}",
        "DEDALUS_X_API_KEY": "${user_config.DEDALUS_X_API_KEY}",
        "DEDALUS_ORG_ID": "${user_config.DEDALUS_ORG_ID}"
      }
    }
  },
  "user_config": {
    "DEDALUS_API_KEY": {
      "title": "api_key",
      "description": "Dedalus API key sent as Authorization: Bearer <key>.",
      "required": false,
      "type": "string"
    },
    "DEDALUS_X_API_KEY": {
      "title": "x_api_key",
      "description": "Dedalus API key sent as x-api-key.",
      "required": false,
      "type": "string"
    },
    "DEDALUS_ORG_ID": {
      "title": "dedalus_org_id",
      "description": "Organization ID header applied to all DCS requests.",
      "required": false,
      "type": "string"
    }
  },
  "tools": [],
  "tools_generated": true,
  "compatibility": {
    "runtimes": {
      "node": ">=18.0.0"
    }
  },
  "keywords": ["api"]
}
